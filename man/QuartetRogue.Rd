% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QuartetRogue.R
\name{QuartetRogue}
\alias{QuartetRogue}
\title{Identify rogue taxa using quartet similarity}
\usage{
QuartetRogue(
  trees,
  neverDrop = character(0),
  maxDrop = NULL,
  return = c("taxa", "tree"),
  p = 0.5,
  verbose = FALSE
)
}
\arguments{
\item{trees}{An object of class \code{multiPhylo}.  All trees must share the same
tip labels.}

\item{neverDrop}{Character vector of tip labels (or integer indices) that
should never be dropped.}

\item{maxDrop}{Maximum number of taxa to drop.
\code{NULL} (default) means drop until no further improvement.}

\item{return}{Character: \code{"taxa"} returns a data frame of drop operations;
\code{"tree"} returns a consensus tree with rogue taxa removed.}

\item{p}{Consensus threshold when \code{return = "tree"}.}

\item{verbose}{Logical; if \code{TRUE}, print progress messages.}
}
\value{
If \code{return = "taxa"}, a \code{data.frame} with columns:
\describe{
\item{num}{Sequential drop index (0 = baseline).}
\item{taxon}{Label of the dropped taxon (\code{NA} for the baseline row).}
\item{rawImprovement}{Score improvement from this drop (\code{NA} for
baseline).}
\item{score}{Mean normalised SimilarityToReference after all drops
so far.}
}
If \code{return = "tree"}, a consensus tree of class \code{phylo} with rogue taxa
removed.
}
\description{
Greedily drops taxa whose removal most improves the mean quartet similarity
across a set of trees.  Uses chance-corrected \code{SimilarityToReference}
(normalised to [0, 1]) as the objective, so that scores are comparable
before and after taxon removal.
}
\details{
At each step the taxon whose removal yields the greatest improvement is
dropped.  A restoration pass then checks whether any earlier drop is now
unnecessary.  The result is a sequence of drops together with the running
score; the optimal reduced tree set is obtained by applying only the drops
up to the score maximum.
}
\section{Method}{

The score for a tree set is the mean of
\code{SimilarityToReference(normalize = TRUE)} over all ordered pairs \verb{(i, j)}
with tree \code{j} as reference.  This score is 0 for random or star trees,
1 for perfect agreement, and comparable across different numbers of taxa.
}

\examples{
library("TreeTools")
trees <- AddTipEverywhere(BalancedTree(6), "rogue")[3:6]
QuartetRogue(trees)
}
\author{
\href{https://orcid.org/0000-0001-5660-1727}{Martin R. Smith}
(\href{mailto:martin.smith@durham.ac.uk}{martin.smith@durham.ac.uk})
}
\concept{tree comparison}
